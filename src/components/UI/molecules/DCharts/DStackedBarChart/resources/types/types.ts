import { type TCriticalAny } from '@core/types/critical-any'

interface IDStackedBarChartProps {
    seriesData: TCriticalAny[]
    xAxisData: string[]
    percent: boolean
}

export type { IDStackedBarChartProps }
